import { Snowflake } from "./Snowflake";
import { User } from "./User";
export declare type GuildMember = {
    user: User;
    nick?: string;
    roles: Snowflake[];
    joinedAt: Date;
    premiumSince?: Date;
    deaf?: boolean;
    mute?: boolean;
};
