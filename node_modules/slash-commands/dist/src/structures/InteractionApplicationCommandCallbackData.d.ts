import { AllowedMentions } from "./AllowedMentions";
import { Embed } from "./Embed";
import { MessageFlags } from "./MessageFlags";
declare type ContentMessage = {
    tts?: boolean;
    content: string;
    embeds?: Embed[];
    allowedMentions?: AllowedMentions;
    flags?: MessageFlags;
};
declare type EmbedMessage = {
    tts?: boolean;
    content?: string;
    embeds: Embed[];
    allowedMentions?: AllowedMentions;
    flags?: MessageFlags;
};
export declare type InteractionApplicationCommandCallbackData = ContentMessage | EmbedMessage;
export {};
